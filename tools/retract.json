{
  "name": "minigraf_retract",
  "description": "Retract a fact from Minigraf's graph memory. Retraction records a new fact with asserted=false — the original stays in history for bi-temporal auditing.",
  "parameters": {
    "type": "object",
    "properties": {
      "facts": {
        "type": "string",
        "description": "A Datalog retract block, e.g. [[:component/auth :calls :component/jwt]]"
      },
      "reason": {
        "type": "string",
        "description": "Why this fact is being retracted. This forces you to justify the removal."
      }
    },
    "required": ["facts", "reason"]
  }
}
